# 2. Equilibria — DKX on real geometry

Same solver, real magnetic fields: VMEC `wout` files and Boozer `.bc` files.
These need an equilibrium file, which is the only thing separating them from
[`../1_basics`](../1_basics).

Next: [`../3_gradients`](../3_gradients) differentiates through the solve.
